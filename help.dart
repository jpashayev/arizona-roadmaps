class HelpModal {
  final String helpName;
  final String subTitle;
  final String helpDescription;
  final String helpContent;

  const HelpModal(
      {this.helpName, this.subTitle, this.helpDescription, this.helpContent});
}
